{
  "name": "task-tamer",
  "version": "0.2.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --recursive --parallel --stream run dev",
    "db:generate": "pnpm --recursive --parallel --stream run generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.13.0"
  }
}
